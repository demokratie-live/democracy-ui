import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { ProcedureListItem } from '../ProcedureListItem';
import { ProcedureListItemStory } from './ProcedureListItem.stories';
import { lightData } from '../../Atoms/PieChart/data';
import { VotesIndex } from '../../Atoms/ListItem/VotesIndex';
import { VoteDate } from '../../Atoms/VoteDate';
import { CommunityChart } from './components/CommunityChart';
import { GovernmentChart } from './components/GovernmentChart';
import { PieChart } from '../../Atoms/PieChart';

<Meta
  title="Molecules/ProcedureListItem"
  component={ProcedureListItem}
  subcomponents={{
    VotesIndex,
    VoteDate,
    CommunityChart,
    GovernmentChart,
    PieChart,
  }}
/>

# Welcome to Storybook Atoms

<Canvas>
  <Story
    name="ProcedureListItem"
    args={{
      title:
        'Forschung zu Kernreaktoren der IV. Generation vorantreiben - Energieversorgung in Deutschland sichern',
      subtitle: 'Energieversorgung in Deutschland sichern',
      voted: true,
      votes: 22736,
      date: new Date('2021-04-15'),
      communityChart: {
        size: 20,
        data: lightData,
      },
      governmentChart: {
        size: 20,
        data: lightData,
      },
    }}
  >
    {ProcedureListItem.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="As Components">
    <ProcedureListItemStory />
  </Story>
</Canvas>

```tsx
    <ProcedureListItem
      communityChart={<PieChart data={lightData} size={20} />}
    />
    // OR

    <ProcedureListItem
      communityChart={{ data: lightData. size: 20 }}
    />
```

<ArgsTable of={ProcedureListItem} />
