import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { VoteDate } from '.';

<Meta
  title="Atoms/VoteDate"
  component={VoteDate}
  argTypes={{
    date: {
      control: { type: 'date' },
    },
    dateEnd: {
      control: { type: 'date' },
    },
  }}
/>

# Welcome to Storybook Atoms

<Canvas>
  <Story name="VoteDate" args={{ date: new Date('2021-04-15') }}>
    {VoteDate.bind({})}
  </Story>
</Canvas>
