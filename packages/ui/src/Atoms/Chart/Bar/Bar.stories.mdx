import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { Bar } from '.';
import { BarStory } from './Bar.stories';
import { lightTheme } from '../../../theme';

<Meta title="Atoms/Charts/Bar" component={Bar} subcomponents={{}} />

# Welcome to Storybook Atoms

<Canvas>
  <Story
    name="WOM"
    args={{
      data: [
        {
          value: 1,
          color: lightTheme.colors.vote.wom.match,
        },
        {
          value: 2,
          color: lightTheme.colors.vote.wom.missmatch,
        },
      ],
      width: 300,
      height: 60,
    }}
  >
    {BarStory.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Party"
    args={{
      data: [
        {
          value: 1,
          color: lightTheme.colors.vote.government.yes,
        },
        {
          value: 2,
          color: lightTheme.colors.vote.government.abstination,
        },
        {
          value: 0.5,
          color: lightTheme.colors.vote.government.no,
        },
      ],
      width: 300,
      height: 60,
    }}
  >
    {BarStory.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Bar} />
