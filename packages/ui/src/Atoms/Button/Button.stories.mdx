import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { Button } from "../Button";
import { ButtonStory } from "./Button.stories";

<Meta
  title="Atoms/Button"
  component={Button}
  argTypes={{
    children: { type: { name: "string", required: false } },
    variant: {
      options: ["primary", "secondary", "danger", "danger-secondary"],
      control: { type: "select" },
    },
  }}
/>

# Welcome to Storybook Atoms

<Canvas>
  <Story
    name="Primary"
    args={{
      children: "Primary",
      variant: "primary",
    }}
  >
    {ButtonStory.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Secondary"
    args={{
      children: "Secondary",
      variant: "secondary",
    }}
  >
    {ButtonStory.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Danger"
    args={{
      children: "Danger",
      variant: "danger",
    }}
  >
    {ButtonStory.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story
    name="Danger Secondary"
    args={{
      children: "Danger Secondary",
      variant: "danger-secondary",
    }}
  >
    {ButtonStory.bind({})}
  </Story>
</Canvas>
