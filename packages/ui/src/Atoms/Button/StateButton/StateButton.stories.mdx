import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { StateButton } from '.';
import { StateButtonStory } from './StateButton.stories';

<Meta title="Atoms/Button/StateButton" component={StateButton} />

# Welcome to Storybook Atoms

<Canvas>
  <Story name="Checked" args={{ state: 'Checked' }}>
    {StateButtonStory.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Add" args={{ state: 'Add' }}>
    {StateButtonStory.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Remove" args={{ state: 'Remove' }}>
    {StateButtonStory.bind({})}
  </Story>
</Canvas>
