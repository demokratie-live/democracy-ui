import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { VoteButton } from '.';
import { VoteButtonStory } from './VoteButton.stories';

<Meta title="Atoms/Button/VoteButton" component={VoteButton} />

# Welcome to Storybook Atoms

<Canvas>
  <Story name="Yes" args={{ decision: 'YES' }}>
    {VoteButtonStory.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="Abstination" args={{ decision: 'ABSTINATION' }}>
    {VoteButtonStory.bind({})}
  </Story>
</Canvas>

<Canvas>
  <Story name="No" args={{ decision: 'NO' }}>
    {VoteButtonStory.bind({})}
  </Story>
</Canvas>
